import { CustomFavStyle } from './custom-fav-style';

describe('CustomFavStyle', () => {
  it('should create an instance', () => {
    const directive = new CustomFavStyle();
    expect(directive).toBeTruthy();
  });
});
