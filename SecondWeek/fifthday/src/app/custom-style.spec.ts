import { CustomStyle } from './custom-style';

describe('CustomStyle', () => {
  it('should create an instance', () => {
    const directive = new CustomStyle();
    expect(directive).toBeTruthy();
  });
});
