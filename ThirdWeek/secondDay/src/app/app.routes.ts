import { Routes } from '@angular/router';
import {AllValidation} from './all-validation/all-validation'
export const routes: Routes = [
    {
        path:'',
        component:AllValidation
    }
];
